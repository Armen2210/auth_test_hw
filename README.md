📘 Auth Test Homework — Django аутентификация

Этот проект создан в рамках выполнения домашних заданий по теме маршрутизация и аутентификация в Django.
Проект реализует базовые страницы, регистрацию, вход и выход пользователя, а также защищённую страницу профиля.

📌 Структура проекта

Проект состоит из Django-проекта auth_test_hw и приложения main, содержащего маршруты, шаблоны и обработчики страниц.

Основные директории:
auth_test_hw/
    auth_test_hw/
        settings.py
        urls.py
    main/
        views.py
        urls.py
        templates/
            base.html
            index.html
            login.html
            logout.html
            register.html
            profile.html

✔️ Задание 1. Маршруты и шаблоны

Реализованы страницы:

/ — главная (index)

/profile/ — профиль (profile)

/login/ — вход (login)

/logout/ — выход (logout)

/register/ — регистрация (register)

В base.html добавлена навигация между страницами.

✔️ Задание 2. Стандартная система аутентификации

Проект использует стандартную модель пользователя Django (User).

Реализовано:
🔹 Регистрация пользователя

Использована стандартная форма:

UserCreationForm


После успешной регистрации — автоматический редирект на страницу входа.

🔹 Вход пользователя

Используется форма:

AuthenticationForm


После успешного входа — редирект в профиль.

🔹 Выход пользователя

Выполняется функцией:

logout(request)


После выхода — редирект на login.

🔹 Страница профиля только для авторизованных пользователей

Реализована через:

LoginRequiredMixin
TemplateView


Неавторизованные пользователи перенаправляются на страницу входа.

🔹 Настройки в settings.py
LOGIN_URL = 'login'
LOGIN_REDIRECT_URL = 'profile'
LOGOUT_REDIRECT_URL = 'login'

🧩 Основные файлы проекта
main/views.py

Содержит обработчики страниц, включая:

регистрацию

вход

выход

защищённый профиль (CBV)

main/urls.py

Определяет маршруты приложения.

Шаблоны (HTML)

Находятся в main/templates/ и используют базовый шаблон base.html.

▶️ Запуск проекта

Создать виртуальное окружение:

python -m venv .venv
.venv\Scripts\activate


Установить зависимости:

pip install django


Применить миграции:

python manage.py migrate


Запустить сервер:

python manage.py runserver


Открыть сайт:

http://127.0.0.1:8000/

🏁 Итог

Проект полностью реализует:

маршрутизацию страниц,

стандартную систему регистрации/входа/выхода,

защищённую страницу профиля,

работу с HTML-шаблонами.

Готов к сдаче преподавателю ✔️